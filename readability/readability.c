#include <cs50.h>
#include <stdio.h>

int main(void)
{
    do
    {
        string text = get_string("Text: \n");
    }
    while();

    int index, L, S, w, t, e;
    L = (t/w);
    S = (e/w);
    index = 0.0588 * L - 0.296 * S - 15.8;
}
