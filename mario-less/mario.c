#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int height = get_int("type a positive number ");
    for (height >=1 && height <= 8);{
    printf("height, %d \n", height);
    }
    do-while
    {
        get_int("type a positive number ");
    }
}
